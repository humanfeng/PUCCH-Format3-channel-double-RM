function input_d = qpsk_mod_pucch(input_b,input_b_len)
%PUCCH特殊的调制，根据协议中的表。5.4.1-1
if pucch_type == 1           %1a
    input_d = 1 - 2 * input_b;
else pucch_type == 2         %1b
    if input_b = [0 0]
        input_d = 
    elseif input_b = [0 1]
        
    elseif input_b = [1 0]
        
    else
        
    end
end
